import {defineStyleConfig} from '@chakra-ui/react';

export default defineStyleConfig({
  baseStyle: {
    zIndex: 1e6,
    borderRadius: 'lg',
    py: 2,
    px: 4,
    fontWeight: 600,
    bg: 'gray.700',
    color: 'gray.50'
  }
});
